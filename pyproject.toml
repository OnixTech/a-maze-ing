[project]
name = "a-maze-ing"
version = "0.2.2"
description = "A maze generation and solving package/program."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []


[tool.ruff]
line-length = 79
[tool.ruff.lint]
select = ["E", "F", "B", "I"]
